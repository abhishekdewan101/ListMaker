import com.adewan.listmaker.models.ListType;

CREATE TABLE AppList (
id TEXT,
title TEXT,
type TEXT AS ListType
);

addList:
INSERT INTO AppList(id, title, type) VALUES ( ?, ?,?);

getAllLists:
SELECT * FROM AppList;

getListForId:
SELECT * FROM AppList WHERE id = ?;